# gator

## Setup

### Config file

Create `.gatorconfig.json` in home directory.

```json
{
  "db_url": "postgres://"
}
```

### Create db

use db migaration running: `npm run migrate`

## Usage

### Commands
